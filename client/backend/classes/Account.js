class Account {
    constructor(name, number, password, orders) {
        this.name = name;
        this.number = number;
        this.password = password;
        this.orders = orders
    }
}
module.exports = Account;
